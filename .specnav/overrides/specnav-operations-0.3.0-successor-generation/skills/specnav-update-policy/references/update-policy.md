# Update Policy

Read this before writing `operations/update-policy.json`.

Updates are current-host scoped by default. Updating every host requires an
explicit user request and evidence for each installation root.
