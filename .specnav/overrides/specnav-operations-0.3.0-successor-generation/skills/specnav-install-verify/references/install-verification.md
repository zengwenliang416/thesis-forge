# Install Verification

Read this before writing `operations/install-verification.json`.

Verify installed plugin surfaces from the installed root, not from source
assumptions:

- marketplace root;
- plugin root;
- plugin name and source;
- target project;
- command used;
- host;
- discovery root check;
- workspace support;
- config status;
- reload requirement.
