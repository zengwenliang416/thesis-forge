# Deploy Plan

Read this before writing `operations/deploy-plan.md`.

Use only when release target is `project-deploy`. Name environment, command,
configuration, secrets, migrations, smoke checks, owner, and deployment window.
