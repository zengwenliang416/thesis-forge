# Static Rubric

Run non-runtime structural checks declared by the plan.

Include when applicable:

- `openspec validate`;
- lint;
- type checks;
- schema checks;
- dependency and lockfile checks;
- banned pattern scans;
- contract script checks.

Missing required tools are blockers with class `tool-unavailable`.
