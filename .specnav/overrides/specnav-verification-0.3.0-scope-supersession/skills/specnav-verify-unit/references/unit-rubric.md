# Unit Rubric

Validate behavior-facing unit and regression coverage.

Check:

- changed public behavior has focused tests;
- edge cases, empty states, error paths, and boundary inputs are covered;
- assertions verify behavior, not private implementation details;
- mocks do not replace the behavior under test;
- tests fail for the right reason when behavior is broken.
