# Regression History

## 2026-08-29 Initial Governed Baseline

The comprehensive DocForge build exposed a missing neutral citation style.
The failure remains recorded and was not deleted.

## 2026-08-29 Neutral Citation E2E

The neutral `docforge-standard` template gained the existing
`GB-T-7714-2025` citation profile. Real inspect, validate, and build checks
then passed without fabricated academic metadata.

## 2026-08-29 Governed Local Prepublication

- npm check passed and `19` tests passed.
- Repository pytest passed with `1380` tests; Ruff and OpenSpec strict passed.
- Trigger routing passed `16/16` with precision and recall `1.0`.
- Output evaluation covers six `file-backed fixture` cases, including one
  near-neighbor and one boundary case.
- The Yao archive has `153` entries and the npm tarball has `43` entries.
- Final source-contract, archive, and npm tarball checksums are recorded in the
  package-external OpenSpec verification receipt. They are not embedded in
  inputs that participate in their own digest.
- Clean tarball install, explicit temporary Codex install, managed update, and
  rollback passed.

Provider evaluation, human review, target-native permission enforcement, real
client telemetry, npm name availability, and publisher authority remain
`missing evidence`. No publication, user-level installation, commit, push, or
release was performed.
