name: PR Gate (migrated)

# Automated pull request and main-branch quality gates run on the self-hosted
# Woodpecker service. Keep this workflow only as a discoverable migration note.

on:
  workflow_dispatch:

jobs:
  migrated:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Automated quality gates run on ci.motion-cover.com via .woodpecker/quality.yml."
