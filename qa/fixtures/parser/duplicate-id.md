# 重复 ID 负例（TF-D2-ID-001） {#chap:dup-case}

两个 figure 使用同一稳定 ID `fig:dup`，validator 必须给出
`duplicate-id` 结构化诊断（AGENTS.md §4：每个语义对象至少考虑 ID 是否重复）。

![第一处重复 ID 的示例图](images/model.png){#fig:dup}

![第二处重复 ID 的示例图](images/model.png){#fig:dup}
