{
  "schema_version": 1,
  "budgets": [
    {
      "area": "archive evidence",
      "limit": "one deterministic test helper locates exactly one archived V1 prototype without writes"
    },
    {
      "area": "headless state",
      "limit": "Python state reference and transport DTOs contain no React, Tauri, HTTP framework, python-docx or lxml imports"
    },
    {
      "area": "component cohesion",
      "limit": "each React panel renders one user-facing responsibility and delegates orchestration to shared workspace state"
    },
    {
      "area": "application reuse",
      "limit": "zero duplicate parser, validator, compiler, numbering, bibliography or renderer implementations in UI modules"
    },
    {
      "area": "async behavior",
      "limit": "one operation-token contract handles stale-result suppression across inspect, validate and build"
    },
    {
      "area": "filesystem safety",
      "limit": "one atomic source writer and the existing atomic DOCX finalizer own replacement behavior"
    },
    {
      "area": "preview boundary",
      "limit": "preview consumes typed domain/render-plan data and never opens DOCX or raw OOXML"
    },
    {
      "area": "runtime boundaries",
      "limit": "React stays under frontend, Tauri stays under src-tauri, and HTTP or sidecar frameworks stay inside Python adapters"
    }
  ]
}
